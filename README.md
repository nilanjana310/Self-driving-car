# Self-driving-car
This project is a simple self-driving car simulation built using the Pygame library in Python. The car automatically follows a predefined track by sensing the path using pixel color detection.
